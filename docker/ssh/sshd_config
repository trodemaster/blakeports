# SSH Proxy Server Configuration
# This sshd accepts connections from modern clients on port 2222

# Listen on all interfaces, port 2222
Port 2222
AddressFamily any
ListenAddress 0.0.0.0
ListenAddress ::

# Use modern host keys for incoming connections
HostKey /etc/ssh/ssh_host_rsa_key
HostKey /etc/ssh/ssh_host_ecdsa_key
HostKey /etc/ssh/ssh_host_ed25519_key

# Logging
SyslogFacility AUTH
LogLevel INFO

# Authentication
PermitRootLogin no
PubkeyAuthentication yes
PasswordAuthentication no
PermitEmptyPasswords no
ChallengeResponseAuthentication no
UsePAM yes

# Authentication methods
AuthenticationMethods publickey

# Authorized keys location
AuthorizedKeysFile .ssh/authorized_keys

# Allow client to pass locale environment variables
AcceptEnv LANG LC_*

# Subsystems
Subsystem sftp /usr/lib/openssh/sftp-server

# Security settings
StrictModes yes
MaxAuthTries 3
MaxSessions 10
LoginGraceTime 60

# Enable TCP forwarding (essential for ProxyJump)
AllowTcpForwarding yes
GatewayPorts no
X11Forwarding no

# Keep connection alive
TCPKeepAlive yes
ClientAliveInterval 30
ClientAliveCountMax 3

# Disable unused features
PermitTunnel no
AllowAgentForwarding yes
AllowStreamLocalForwarding no
PermitUserEnvironment no

# Performance
Compression delayed
UseDNS no

