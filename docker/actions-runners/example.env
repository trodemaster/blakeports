# GitHub Actions Runner Configuration
# Copy this file to .env and fill in your values

# GitHub repository information
GITHUB_OWNER=trodemaster
GITHUB_REPO=blakeports

# Runner registration token (generate on host, expires in 1 hour)
# Generate using: gh api repos/$GITHUB_OWNER/$GITHUB_REPO/actions/runners/registration-token --method POST --jq '.token'
# Or use the provided script: ./generate-token.sh
RUNNER_TOKEN=

# Runner configuration
RUNNER_NAME=docker-runner-1
RUNNER_WORKDIR=_work

# GitHub Actions runner version (optional, defaults to latest tested version)
# Check releases at: https://github.com/actions/runner/releases
RUNNER_VERSION=2.321.0

# Custom labels (optional, comma-separated)
# Default labels are automatically added: self-hosted,Linux,docker,ssh-capable,X64/ARM64,ubuntu-XX.XX
CUSTOM_LABELS=

# SSH key path for VM access (optional, defaults to ~/.ssh)
# This directory will be mounted as /home/runner/.ssh in the container
SSH_KEY_PATH=~/.ssh

# Notes:
# - The .env file is ignored by git for security
# - Registration tokens expire after 1 hour, regenerate as needed
# - Ensure SSH keys have appropriate permissions (600 for private keys)
