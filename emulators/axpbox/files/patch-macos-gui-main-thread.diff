--- src/SystemComponent.hpp.orig
+++ src/SystemComponent.hpp
@@ -51,6 +51,11 @@
   virtual void start_threads(){};
   virtual void stop_threads(){};
 
+  /* Run one iteration of the component's GUI loop on the calling thread.
+     Returns true if a step was executed. Used on macOS, where Cocoa
+     requires all GUI calls to happen on the process main thread. */
+  virtual bool run_gui_step() { return false; };
+
   char *devid_string;
 
 protected:
--- src/System.cpp.orig
+++ src/System.cpp
@@ -275,7 +275,19 @@
     if (ProcessPendingReset())
       continue;
 
-    std::this_thread::sleep_for(std::chrono::milliseconds(100));
+    bool gui_stepped = false;
+#if defined(__APPLE__)
+    /* On macOS, Cocoa requires all GUI calls to happen on the process
+       main thread, so drive the VGA card's GUI loop from here rather
+       than from a device thread. A GUI step sleeps ~100ms internally,
+       preserving this loop's cadence. */
+    for (i = 0; i < iNumComponents; i++) {
+      if (acComponents[i]->run_gui_step())
+        gui_stepped = true;
+    }
+#endif
+    if (!gui_stepped)
+      std::this_thread::sleep_for(std::chrono::milliseconds(100));
     for (i = 0; i < iNumComponents; i++)
       acComponents[i]->check_state();
 #if !defined(HIDE_COUNTER)
--- src/Cirrus.hpp.orig
+++ src/Cirrus.hpp
@@ -65,6 +65,7 @@
   virtual void init();
   virtual void start_threads();
   virtual void stop_threads();
+  virtual bool run_gui_step();
 
   virtual u8 get_actl_palette_idx(u8 index);
   virtual void redraw_area(unsigned x0, unsigned y0, unsigned width,
@@ -122,6 +123,7 @@
   std::unique_ptr<std::thread> myThread;
   std::atomic_bool myThreadDead{false};
   bool StopThread;
+  bool GuiInitialized = false;
 
   /// The state structure contains all elements that need to be saved to the
   /// statefile
--- src/Cirrus.cpp.orig
+++ src/Cirrus.cpp
@@ -344,11 +344,53 @@
  * Create and start thread.
  **/
 void CCirrus::start_threads() {
+#if defined(__APPLE__)
+  /* Cocoa requires all GUI calls on the process main thread; the GUI
+     loop is driven from CSystem::Run() via run_gui_step() instead of
+     a device thread. Initialize the GUI eagerly here (start_threads
+     runs on the main thread) so the SDL screen exists before the CPU
+     threads are released and the guest VGA BIOS starts programming
+     the card. */
+  printf(" cirrus(main)");
+  StopThread = false;
+  if (!GuiInitialized) {
+    bx_gui->init(state.x_tilesize, state.y_tilesize);
+    GuiInitialized = true;
+  }
+#else
   if (!myThread) {
     printf(" cirrus");
     StopThread = false;
     myThread = std::make_unique<std::thread>([this](){ this->run(); });
+  }
+#endif
+}
+
+/**
+ * Run one iteration of the GUI loop on the calling thread. This is
+ * one pass of the run() loop body, used on macOS where the GUI must
+ * be driven from the process main thread.
+ **/
+bool CCirrus::run_gui_step() {
+  if (StopThread)
+    return false;
+  if (!GuiInitialized) {
+    bx_gui->init(state.x_tilesize, state.y_tilesize);
+    GuiInitialized = true;
+  }
+  // Handle GUI events (100 times per second)
+  for (int i = 0; i < 10; i++) {
+    bx_gui->lock();
+    bx_gui->handle_events();
+    bx_gui->unlock();
+    std::this_thread::sleep_for(std::chrono::milliseconds(10));
   }
+  // Update the screen (10 times per second)
+  bx_gui->lock();
+  update();
+  bx_gui->flush();
+  bx_gui->unlock();
+  return true;
 }
 
 /**
--- src/S3Trio64.hpp.orig
+++ src/S3Trio64.hpp
@@ -104,6 +104,7 @@
   virtual void init() override;
   virtual void start_threads() override;
   virtual void stop_threads() override;
+  virtual bool run_gui_step() override;
 
 protected:
   virtual u16 line_compare_mask() override;
@@ -396,6 +397,7 @@
   std::unique_ptr<std::thread> myThread;
   std::atomic_bool myThreadDead{false};
   bool StopThread;
+  bool GuiInitialized = false;
 
   /// The state structure contains all elements that need to be saved to the
   /// statefile.
--- src/S3Trio64.cpp.orig
+++ src/S3Trio64.cpp
@@ -2746,6 +2746,21 @@
  * Create and start thread.
  **/
 void CS3Trio64::start_threads() {
+#if defined(__APPLE__)
+  /* Cocoa requires all GUI calls on the process main thread; the GUI
+     loop is driven from CSystem::Run() via run_gui_step() instead of
+     a device thread. Initialize the GUI eagerly here (start_threads
+     runs on the main thread) so the SDL screen exists before the CPU
+     threads are released and the guest VGA BIOS starts programming
+     the card. */
+  printf(" s3(main)");
+  StopThread = false;
+  PauseThread.store(false, std::memory_order_release);
+  if (!GuiInitialized) {
+    bx_gui->init(state.x_tilesize, state.y_tilesize);
+    GuiInitialized = true;
+  }
+#else
   // Resume after reset if the thread already exists
   PauseThread.store(false, std::memory_order_release);
 
@@ -2754,12 +2769,26 @@
     StopThread = false;
     myThread = std::make_unique<std::thread>([this]() { this->run(); });
   }
+#endif
 }
 
 /**
  * Stop and destroy thread.
  **/
 void CS3Trio64::stop_threads() {
+#if defined(__APPLE__)
+  /* During firmware reset, keep driving the GUI from run_gui_step()
+     (it owns the SDL window) instead of tearing it down; just mark
+     the pause state so run_gui_step() stops touching emulated VGA
+     state, mirroring the non-macOS thread-pause behavior below. */
+  if (cSystem && cSystem->IsResetInProgress()) {
+    PauseThread.store(true, std::memory_order_release);
+    printf(" s3(pause)");
+    return;
+  }
+  StopThread = true;
+  return;
+#else
   // During firmware reset, do NOT kill the S3 thread (it owns the SDL window).
   // Just pause it so the window stays alive.
   if (cSystem && cSystem->IsResetInProgress()) {
@@ -2786,9 +2815,57 @@
     myThread->join();
     myThread = nullptr;
   }
+#endif
 }
 
 /**
+ * Run one iteration of the GUI loop on the calling thread. This is
+ * one pass of the run() loop body, used on macOS where the GUI must
+ * be driven from the process main thread. Mirrors run()'s firmware
+ * reset pause handling so the SDL window survives a reset here too.
+ **/
+bool CS3Trio64::run_gui_step() {
+  if (StopThread)
+    return false;
+  if (!GuiInitialized) {
+    bx_gui->init(state.x_tilesize, state.y_tilesize);
+    GuiInitialized = true;
+  }
+
+  static bool was_paused = false;
+
+  // Handle GUI events (100 times per second)
+  for (int i = 0; i < 10; i++) {
+    bx_gui->lock();
+    bx_gui->handle_events();
+    bx_gui->unlock();
+    std::this_thread::sleep_for(std::chrono::milliseconds(10));
+  }
+
+  // During firmware reset: keep pumping events (window stays alive),
+  // but do NOT touch emulated VGA state.
+  if (PauseThread.load(std::memory_order_acquire)) {
+    if (!was_paused) {
+      bx_gui->lock();
+      bx_gui->clear_screen();
+      bx_gui->unlock();
+      was_paused = true;
+    }
+    PauseAck.store(true, std::memory_order_release);
+    return true;
+  }
+  PauseAck.store(false, std::memory_order_release);
+  was_paused = false;
+
+  // Update the screen (10 times per second)
+  bx_gui->lock();
+  update();
+  bx_gui->flush();
+  bx_gui->unlock();
+  return true;
+}
+
+/**
  * Destructor.
  **/
 CS3Trio64::~CS3Trio64() { stop_threads(); }
