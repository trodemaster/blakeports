name: Build bstring

on:
  push:
    paths:
      - 'textproc/bstring/**'
    branches: [main]
  pull_request:
    paths:
      - 'textproc/bstring/**'
    branches: [main]
  workflow_dispatch:

jobs:
  build-bstring:
    name: build-bstring (${{ matrix.runner }})
    strategy:
      matrix:
        runner: [macOS_15, macOS_26_Beta]
      fail-fast: false
    runs-on: ${{ matrix.runner }}
    
    steps:
    - name: Show macOS version
      run: |
        echo "Running on: ${{ matrix.runner }}"
        sw_vers
        
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Setup MacPorts and BlakePorts Environment
      run: |
        ./scripts/installmacports

    - name: Clean existing bstring installation
      run: |
        # Source MacPorts environment from our local copy
        source ./setupenv.bash

        # Check if any version of bstring is installed
        echo "Checking for existing bstring installations..."
        if port installed bstring >/dev/null 2>&1; then
          echo "📋 Current bstring installations:"
          port installed bstring

          echo ""
          echo "🗑️  Removing ALL existing bstring installation(s)..."

          # Force uninstall all versions of bstring
          if sudo port -f uninstall installed and name=bstring; then
            sudo port clean bstring
            echo "✅ All existing bstring installation(s) removed successfully"

            # Verify cleanup was successful
            echo ""
            echo "🔍 Verifying cleanup..."
            if port installed bstring >/dev/null 2>&1; then
              echo "⚠️  Warning: Some bstring installations may still exist"
              port installed bstring
            else
              echo "✅ Confirmed: No bstring installations remain"
            fi
          else
            echo "❌ Failed to uninstall bstring"
            exit 1
          fi
        else
          echo "ℹ️  No existing bstring installation found"
        fi

    - name: Install dependencies
      run: |
        ./scripts/install-deps bstring
        
    - name: Build bstring port
      run: |
        # Source MacPorts environment from our local copy
        source ./setupenv.bash
        
        sudo port -v install bstring
        
    - name: Verify bstring installation
      run: |
        # Source MacPorts environment from our local copy
        source ./setupenv.bash
        
        port installed bstring
        pkg-config --exists bstring && echo "bstring pkg-config found" || echo "bstring pkg-config not found"
        
    - name: Test bstring with tests variant
      run: |
        # Source MacPorts environment from our local copy
        source ./setupenv.bash

        # Test with tests variant
        sudo port -v install bstring +tests

    - name: Final cleanup - Remove all bstring installations
      run: |
        # Source MacPorts environment from our local copy
        source ./setupenv.bash

        echo "🧹 Performing final cleanup of all bstring installations..."
        if port installed bstring >/dev/null 2>&1; then
          echo "📋 Final bstring installations before cleanup:"
          port installed bstring

          echo ""
          echo "🗑️  Removing ALL bstring installations for clean state..."

          # Final comprehensive cleanup
          if sudo port -f uninstall installed and name=bstring; then
            sudo port clean --all bstring
            echo "✅ Final cleanup completed successfully"

            # Final verification
            if port installed bstring >/dev/null 2>&1; then
              echo "⚠️  Final verification: Some installations may remain"
              port installed bstring
              exit 1
            else
              echo "🎉 Final verification: Clean state confirmed - no bstring installations remain"
            fi
          else
            echo "❌ Final cleanup failed"
            exit 1
          fi
        else
          echo "ℹ️  No bstring installations found - already clean"
        fi
        