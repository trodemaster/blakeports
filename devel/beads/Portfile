# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4

PortSystem          1.0
PortGroup           golang 1.0

go.setup            github.com/steveyegge/beads 0.49.3 v
go.offline_build    no
revision            0

homepage            https://github.com/steveyegge/beads
categories          devel
maintainers         {@trodemaster icloud.com:manuals-unread2u} openmaintainer
license             MIT

description         \
    Distributed, git-backed graph issue tracker for AI agents

long_description    \
    Beads provides a persistent, structured memory for coding agents. \
    It replaces messy markdown plans with a dependency-aware graph, \
    allowing agents to handle long-horizon tasks without losing context. \
    CLI is bd. Issues stored as JSONL in .beads/, versioned with git.

# Build the bd binary from cmd/bd (not the default package root)
build.target        ./cmd/bd

checksums           rmd160  bd61afd0fd0fb6af843e2c35f7ef56a726e36feb \
                    sha256  88e5a72fb7820887d5e5bb0ebf414f1704de385ca2097e3e05149333351ae5f7 \
                    size    3794728

# Binary is named bd when building ./cmd/bd
destroot {
    xinstall -m 0755 ${worksrcpath}/bd ${destroot}${prefix}/bin/bd
}
