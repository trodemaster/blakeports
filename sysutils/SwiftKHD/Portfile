# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4

PortSystem          1.0

name                SwiftKHD
version             0.1.0
revision            0

categories          sysutils
platforms           {darwin >= 22}
license             MIT
maintainers         {netjibbing.com:blake} openmaintainer

description         Simple hotkey daemon for macOS written in Swift
long_description    SwiftKHD is a Swift port of skhd, a simple hotkey daemon \
                    for macOS. Fully compatible with existing skhd configuration \
                    files. Features modal hotkeys, process-specific bindings, key \
                    forwarding, media key support, and live config hot reload. \
                    Service management uses the native SMAppService API (macOS 13+).

homepage            https://github.com/trodemaster/SwiftKHD

conflicts           skhd skhd.zig

fetch.type          git
git.url             https://github.com/trodemaster/SwiftKHD
git.branch          main

use_configure       no

build {
    # MacPorts' macports-user HOME is not writable inside its sandbox, causing SPM to
    # fall back to re-compiling Package.swift under sandbox-exec — which fails because
    # nested sandbox-exec is not permitted on macOS. Providing a writable HOME lets
    # SPM cache the manifest and avoid the nested sandbox path entirely.
    system "cd ${worksrcpath} && HOME=${workpath} swift build -c release"
}

destroot {
    xinstall -d ${destroot}${prefix}/bin
    # .build/release is a symlink SPM creates pointing to the native-arch release dir
    xinstall -m 755 ${worksrcpath}/.build/release/SwiftKHD \
        ${destroot}${prefix}/bin/swiftkHD

    xinstall -d ${destroot}${prefix}/share/doc/${name}
    xinstall -m 644 ${worksrcpath}/README.md \
        ${destroot}${prefix}/share/doc/${name}
    xinstall -m 644 ${worksrcpath}/LICENSE \
        ${destroot}${prefix}/share/doc/${name}
}

notes "
To get started:

1. Install the launchd service:
   swiftkHD --install-service

2. Grant accessibility permissions:
   System Settings > Privacy & Security > Accessibility > add SwiftKHD

3. Create your config at ~/.config/skhd/skhdrc (same format as skhd)

To reload config while running:
   swiftkHD --reload

To check service status:
   swiftkHD --status
"
